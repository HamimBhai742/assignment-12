import React from 'react';

const HeadingTitle = ({ value }) => {
    return (
        <div>
            <h3 className='font-cinzel text-3xl font-bold mt-8 mb-3 text-center'>{value}</h3>
        </div>
    );
};

export default HeadingTitle;